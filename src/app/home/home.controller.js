// =============================================================================
// Home Controller
// =============================================================================


app.controller('HomeController',
    [() => {

        /**
         * Define the public object for the controller
         *
         * @type  {Object}
         */
        let HomeController = {
            title: 'Home'
        };

        return HomeController;

    }]
);
